# Requirements Quality Checklist: Data Integrity — Lossless CST Core Engine
**Created**: 2026-06-11 | **Feature**: [spec.md](../spec.md)

## Byte-for-Byte Round-Trip

- [X] CHK001 Is the round-trip identity (output bytes == input bytes for an unmodified tree) defined as a measurable, unambiguous equality rather than a "preserves formatting" generality? [Testability, Spec §TR-003 / INV-2] <!-- Evaluator: Covered by spec.md §TR-003 + data-model.md INV-2 (byte-for-byte concatenated-token equality) -->
- [X] CHK002 Do the requirements state the exact comparison granularity for round-trip (byte sequence vs. normalized text) so two reviewers would measure it identically? [Clarity, Spec §SC-001] <!-- Evaluator: Covered by spec.md §SC-001 ("byte-for-byte") + INV-1/INV-2 (byte-level, not normalized) -->
- [X] CHK003 Is the requirement explicit that round-trip MUST hold for error-recovered trees, not only well-formed input? [Completeness, Spec §TR-005 / Constraints / INV-3] <!-- Evaluator: Covered by spec.md §Technical Constraints ("including error-recovered trees") + TR-005 + data-model.md INV-3 -->
- [X] CHK004 Are the verification methods for round-trip (corpus, property, fuzz) each tied to a distinct, stated success threshold (e.g., 100% of corpus files) rather than left implicit? [Testability, Spec §SC-001/SC-002/SC-003] <!-- Evaluator: Covered by spec.md §SC-001 (100% corpus) / SC-002 (every TR-004 construct) / SC-003 (no panics + token concat == input) -->
- [X] CHK005 Is the round-trip domain boundary stated unambiguously — that it applies to every accepted UTF-8 input and explicitly excludes non-UTF-8 input? [Consistency, Spec §Clarifications Q2 / TR-001 / STF-001] <!-- Evaluator: Covered by spec.md §Technical Constraints + TR-001 + STF-001 note + data-model.md INV-4 -->
- [X] CHK006 Does the spec define what "100% of files byte-for-byte" means when a corpus file itself is malformed (does the recovered tree still count toward the 100%)? [Clarity, Spec §SC-001/SC-003] <!-- Evaluator: Resolved — clarified SC-001 in spec.md to state malformed corpus files count toward the 100% via their error-recovered trees (INV-3) -->

## Trivia, Comment, Ordering & Struct-Name Preservation

- [X] CHK007 Is the complete set of preserved trivia/structural elements enumerated (comments, whitespace, trailing commas, struct/variant names, ordering) with no "etc." gaps? [Completeness, Spec §TR-002] <!-- Evaluator: Covered by spec.md §TR-002 (absolute "retain every source byte" closes any gap) + Scope enumeration + Objective 1 VC2 (ordering) -->
- [X] CHK008 Is the trivia attachment rule specified as a single deterministic rule rather than multiple possible interpretations? [Consistency, Spec §AD-001 / Trivia entity] <!-- Evaluator: Covered by plan.md §AD-001 + data-model.md Trivia row ("Exactly one attachment rule applied consistently everywhere") -->
- [X] CHK009 Are leading vs. trailing trivia binding behaviors each defined explicitly, including trailing trivia at end-of-input? [Completeness, Spec §AD-001 / data-model Trivia row] <!-- Evaluator: Covered by plan.md §AD-001 + data-model.md Trivia row (leading binds following token; trailing-at-EOF binds nearest preceding structure) -->
- [X] CHK010 Is key/field ordering preservation stated as a requirement and not merely implied by "preserve formatting"? [Clarity, Spec §TR-002 / Principle I] <!-- Evaluator: Covered by spec.md §Objective 1 VC2 ("ordering ... preserved verbatim") + data-model.md SyntaxNode ("Child order is source order") + TR-002 -->
- [X] CHK011 Are CRLF vs. LF line endings and a missing trailing newline each called out as elements that must be preserved exactly? [Completeness, Spec §Edge Cases / TR-002] <!-- Evaluator: Covered by spec.md §Edge Cases ("Missing trailing newline; CRLF vs LF ... preserved exactly") + data-model.md Trivia row -->
- [X] CHK012 Does the requirement set distinguish tuples, lists/sequences, and maps (with non-string keys) as separately preserved constructs rather than collapsible into one? [Clarity, Spec §TR-004 / data-model SyntaxKind note] <!-- Evaluator: Covered by spec.md §TR-004 ("distinct, preserved constructs") + data-model.md SyntaxKind note (non-string keys not collapsed) -->

## Non-Destructive Edit Primitives

- [X] CHK013 Is "unaffected regions print byte-identically" defined precisely enough to be testable after each edit primitive? [Testability, Spec §TR-011 / SC-007 / INV-8] <!-- Evaluator: Covered by spec.md §TR-011 + SC-007 ("unaffected regions print byte-identically") + data-model.md INV-8 -->
- [X] CHK014 Are the edit primitive granularities (syntax-node and token-span) both stated as required, with no ambiguity over which the caller may choose? [Completeness, Spec §TR-011 / AD-004] <!-- Evaluator: Covered by spec.md §TR-011 ("both syntax-node and token-span granularity") + plan.md AD-004 + data-model.md EditOperation -->
- [X] CHK015 Is the caller-chosen trivia policy enumerated to its full option set (keep/discard × leading/trailing) so each combination is testable? [Clarity, Spec §Clarifications Q4 / data-model EditOperation row] <!-- Evaluator: Covered by spec.md §Clarifications Q4 + data-model.md EditOperation ("keep/discard leading, keep/discard trailing") -->
- [X] CHK016 Does the spec state the non-destructiveness guarantee (original tree remains valid/round-trippable after an edit produces a new tree)? [Completeness, Spec §data-model Lifecycle / INV-8] <!-- Evaluator: Covered by data-model.md §State/Lifecycle ("EditOperation yields a new tree ... original CstDocument remains valid and round-trippable") + INV-8 -->
- [X] CHK017 Is there a requirement that, after any edit, the tree contains no un-covered or overlapping byte ranges? [Consistency, Spec §data-model EditOperation row / INV-1] <!-- Evaluator: Covered by data-model.md EditOperation ("No edit may leave the tree with un-covered or overlapping byte ranges") + INV-1 -->
- [X] CHK018 Is the post-edit invariant "the whole tree remains printable" stated as a verifiable acceptance condition? [Testability, Spec §TR-011 / SC-007] <!-- Evaluator: Covered by spec.md §TR-011 ("tree MUST remain printable") + SC-007 ("full tree remains printable") -->

## Error-Recovery Completeness

- [X] CHK019 Is the coverage guarantee — token texts concatenated equal the entire input — stated as an absolute invariant for any accepted input? [Completeness, Spec §SC-003 / INV-1] <!-- Evaluator: Covered by spec.md §SC-003 (token concat == input for every accepted UTF-8 input) + data-model.md INV-1 + TR-005 -->
- [X] CHK020 Are the recovery representations (ERROR nodes for unexpected tokens, missing/empty nodes for absent constructs) each defined so a reviewer can confirm all input is covered? [Clarity, Spec §Objective 2 deliverables / TR-005] <!-- Evaluator: Covered by spec.md §Objective 2 deliverables ("ERROR nodes" + "missing/empty nodes") + TR-005 -->
- [X] CHK021 Is the no-panic guarantee stated unambiguously for all byte sequences, including arbitrary/malformed input? [Testability, Spec §TR-001/TR-005 / SC-003] <!-- Evaluator: Covered by spec.md §TR-001 + TR-005 ("without panicking") + SC-003 ("Fuzzing finds no panics on arbitrary input") -->
- [X] CHK022 Does the spec require parser termination on all input with a stated mechanism (must-consume-a-token invariant) rather than asserting termination without justification? [Completeness, Spec §TR-014 / HINT-004] <!-- Evaluator: Covered by spec.md §TR-014 ("MUST terminate on all input") + plan.md HINT-004 ("every parser loop iteration must consume ≥1 token") -->
- [X] CHK023 Is the single-token byte-coverage invariant (every byte in exactly one token, none double-counted) stated explicitly, not just "covers all input"? [Clarity, Spec §INV-1 / data-model SyntaxToken row] <!-- Evaluator: Covered by data-model.md INV-1 + SyntaxToken row ("every source byte belongs to exactly one token (no byte un-tokenized, none double-counted)") -->

## UTF-8 Input Contract, BOM & Non-UTF-8 Rejection

- [X] CHK024 Is the input acceptance contract (valid UTF-8, optionally BOM-prefixed) stated precisely at the API boundary? [Clarity, Spec §TR-001 / AD-008] <!-- Evaluator: Covered by spec.md §TR-001 ("accept any valid UTF-8 input (optionally prefixed with a UTF-8 BOM)") + plan.md AD-008 -->
- [X] CHK025 Is non-UTF-8 rejection defined as a clean error with no partial/garbled load and explicitly never a panic? [Completeness, Spec §TR-001 / Edge Cases / INV-4] <!-- Evaluator: Covered by spec.md §TR-001 + Edge Cases ("clean error ... never a partial or garbled load") + data-model.md INV-4 -->
- [X] CHK026 Is BOM handling specified unambiguously — preserved as leading trivia and round-tripped, not stripped? [Consistency, Spec §Clarifications Q8 / data-model Trivia row] <!-- Evaluator: Covered by spec.md §Clarifications Q8 + data-model.md Trivia row ("leading UTF-8 BOM is preserved as leading trivia") -->
- [X] CHK027 Is it stated that non-UTF-8 input is never represented in the tree (excluded from the model entirely)? [Consistency, Spec §INV-4 / TR-001] <!-- Evaluator: Covered by data-model.md INV-4 ("never represented in the tree; the tree models accepted UTF-8 only") + TR-001 -->
- [X] CHK028 Does the requirement set reconcile "parse any byte sequence" language with the UTF-8-only contract so there is no contradiction across sections? [Consistency, Spec §STF-001 / Clarifications Q8] <!-- Evaluator: Covered by spec.md §Stress-Test Findings STF-001 note (round-trip scoped to "every accepted (UTF-8) input") + Clarifications Q8 -->

## Deterministic Parsing

- [X] CHK029 Is determinism defined to the level of identical tree shape, kinds, child order, and byte ranges — not merely "same output"? [Clarity, Spec §TR-012 / INV-6] <!-- Evaluator: Covered by data-model.md INV-6 ("identical tree (same kinds, order, ranges)") + TR-012 -->
- [X] CHK030 Does the determinism requirement also cover identical diagnostics for identical input? [Completeness, Spec §INV-6] <!-- Evaluator: Covered by data-model.md INV-6 ("and identical diagnostics") -->
- [X] CHK031 Is determinism stated in a way that is independently verifiable (e.g., repeated-parse equality) without referencing implementation internals? [Testability, Spec §TR-012] <!-- Evaluator: Covered by spec.md §TR-012 (observable input→output equality, no impl internals) + INV-6 (observable properties) -->

## Diagnostic Byte-Range Accuracy

- [X] CHK032 Is "precise source byte range" defined as a concrete `[start, end)` form so range correctness is checkable per diagnostic? [Clarity, Spec §TR-006 / data-model Diagnostic row] <!-- Evaluator: Covered by spec.md §TR-006 + data-model.md Diagnostic row ("range: ByteRange (precise source [start, end))") -->
- [X] CHK033 Is there a stated constraint that every diagnostic range be a valid sub-range of `[0, source_len)`? [Consistency, Spec §data-model Diagnostic row / TR-006] <!-- Evaluator: Covered by data-model.md Diagnostic row ("range MUST be a valid sub-range of [0, source_len)") -->
- [X] CHK034 Is the "one diagnostic per recovery point" rule defined unambiguously (no duplicate diagnostics for the same recovery event)? [Clarity, Spec §TR-013 / AD-003] <!-- Evaluator: Covered by spec.md §TR-013 + plan.md AD-003 + data-model.md Diagnostic row ("not duplicated for the same recovery event") -->
- [X] CHK035 Are the diagnostic severity enum (Error/Warning) and the namespaced code registry (RON-Pxxxx) defined as a stable, closed contract rather than open-ended strings? [Completeness, Spec §TR-013 / AD-003] <!-- Evaluator: Covered by spec.md §TR-013 ("fixed severity enum ... stable, namespaced code registry ... part of the stable public API") + data-model.md Diagnostic ("codes come from a fixed registry") -->
- [X] CHK036 Is it stated that the presence of diagnostics never alters the tree's byte coverage or round-trip result? [Consistency, Spec §data-model Diagnostic row / INV-3] <!-- Evaluator: Covered by data-model.md Diagnostic row ("Diagnostics never alter the tree's byte coverage ... does not change round-trip") + INV-3 -->
- [X] CHK037 Is the depth-limit diagnostic requirement (emit a diagnostic when nesting exceeds the documented bound) defined with a stated, documented bound rather than an arbitrary one? [Testability, Spec §TR-014 / SC-008 / INV-5] <!-- Evaluator: Covered by spec.md §TR-014 ("documented nesting/recursion bound ... emits a diagnostic when input exceeds it") + SC-008 + data-model.md INV-5 + plan.md AD-005 (configurable guard) -->

## Cross-Cutting Consistency & Traceability

- [X] CHK038 Does every data-integrity success criterion (SC-001..SC-008) trace to at least one technical requirement and at least one invariant (INV-1..INV-8)? [Consistency, Spec §Requirement Coverage Map / data-model INV table] <!-- Evaluator: Resolved — added a Requirement Coverage Map (SC→TR→INV) to spec.md §Success Criteria and added INV-9 (WASM-clean portability) to data-model.md so SC-005 has invariant backing; all SC-001..SC-008 now trace to ≥1 TR and ≥1 INV -->
- [X] CHK039 Are terms "lossless", "trivia", and "round-trip" used consistently with their Glossary definitions across spec, plan, and data-model? [Clarity, Spec §Glossary / STF-001] <!-- Evaluator: Covered by spec.md §Glossary (lossless/trivia/round-trip definitions) used consistently in plan.md and data-model.md -->
- [X] CHK040 Is the grammar-surface authority (pinned `ron` 0.12.1) stated as the single definition of in-scope constructs so "full RON grammar surface" is bounded and testable? [Completeness, Spec §TR-004 / AD-002] <!-- Evaluator: Covered by spec.md §TR-004 ("whatever the pinned ron crate version accepts") + plan.md AD-002 (pin ron = "=0.12.1") + Technical Context -->
