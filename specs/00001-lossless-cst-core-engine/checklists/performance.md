# Performance: Lossless CST Core Engine
**Created**: 2026-06-11 | **Feature**: [spec.md](../spec.md)

## Correctness-Only Envelope Clarity

- [X] CHK001 Is the "correctness-only" performance envelope stated unambiguously, such that a reader cannot infer any latency or throughput obligation for E001? [Clarity, Spec §TR-012/Constraints, plan §Performance Goals] <!-- Evaluator: Covered by spec.md §Technical Constraints ("commits to correctness, not a performance threshold") and plan.md §Performance Goals ("no latency/throughput threshold") -->
- [X] CHK002 Are the only two committed non-functional guarantees (termination on any input and bounded nesting depth) enumerated in exactly one authoritative place, with other sections referencing rather than restating them? [Consistency, Spec §Constraints, plan §Performance Goals] <!-- Evaluator: Covered by spec.md §Technical Constraints (authoritative enumeration); Clarifications Q6, Obj2, and plan §Performance Goals state the same two guarantees with no divergent wording -->
- [X] CHK003 Does the spec explicitly disclaim any latency/throughput/memory threshold for this epic so reviewers do not read the project-wide Performance Standards (16 ms frame, 100k rows) as in-scope here? [Clarity, plan §Performance Goals, project-instructions §Performance Standards] <!-- Evaluator: Resolved — added explicit disclaimer to spec.md §Technical Constraints and plan.md §Performance Goals that project-wide Performance Standards (~16 ms, 100k rows) are desktop-editor targets (E003+), not in scope for E001 -->
- [X] CHK004 Is the relationship between the correctness-only envelope and the deferred interactive/incremental performance work (later epics) stated so the boundary is unmistakable? [Completeness, Spec §Excluded, plan §Scale/Scope] <!-- Evaluator: Resolved — disclaimer added in spec.md §Technical Constraints / plan §Performance Goals ties the correctness-only envelope to later-epic interactive perf (E003+); spec §Excluded defers incremental reparse -->
- [X] CHK005 Is the term "envelope" defined or used consistently, distinguishing a *correctness* envelope from a *performance budget*? [Clarity, Spec §Clarifications Q6] <!-- Evaluator: Covered by Clarifications Q6 ("Performance/size envelope -> Correctness-only"); "correctness-only" used consistently with no conflicting "performance budget"/threshold promise anywhere -->

## Bounded Nesting Depth / No Stack Overflow

- [X] CHK006 Is the bounded-nesting-depth requirement (TR-014) stated as a hard MUST rather than an aspiration? [Clarity, Spec §TR-014] <!-- Evaluator: Covered by spec.md §TR-014 ("MUST terminate... MUST NOT overflow the stack... enforces a documented nesting/recursion bound") -->
- [X] CHK007 Does the requirement specify that the nesting bound is *documented* (a concrete, discoverable limit) rather than merely "reasonable" or implementation-defined-without-record? [Testability, Spec §TR-014, data-model §INV-5] <!-- Resolved: bound set to a documented default of 128 (configurable) in TR-014, SC-008, AD-005, INV-5 -->
- [X] CHK008 Is the observable behavior on exceeding the bound specified precisely (a diagnostic is emitted, parsing stops descending, the tree still round-trips) rather than left to inference? [Completeness, Spec §TR-014/SC-008, plan §Error Handling] <!-- Evaluator: Covered by plan §Error Handling ("stop descent; emit depth-limit Diagnostic; tree still round-trips") + TR-014/SC-008 -->
- [X] CHK009 Is the diagnostic emitted at the depth limit tied to the diagnostic contract (severity enum, RON-Pxxxx code, byte range) so the over-limit signal is verifiable like any other diagnostic? [Consistency, Spec §TR-013/TR-014] <!-- Evaluator: Covered — the over-limit signal is a Diagnostic entity governed by TR-013 (severity enum + RON-Pxxxx + byte range); plan §Requirement Coverage Map TR-014 row now explicitly references the TR-013 contract -->
- [X] CHK010 Does the requirement distinguish "no stack overflow" (a crash-safety guarantee) from "accepts arbitrarily deep input" (which it does not), avoiding a reader assuming unlimited depth? [Clarity, Spec §TR-014/SC-008] <!-- Evaluator: Covered — TR-014/SC-008 enforce a bound and emit a diagnostic past it, so unlimited depth is explicitly not accepted -->
- [X] CHK011 Is the nesting bound expressed in terms a tester can measure (e.g., applies to both well-formed and malformed deeply nested input) rather than only one input class? [Coverage, Spec §SC-008, data-model §INV-5] <!-- Evaluator: Covered — TR-014 applies to "all input" and SC-003 to "arbitrary input" (valid or malformed); INV-5 now states the guard applies regardless of validity -->
- [X] CHK012 Is INV-5 (bounded nesting) reconciled with INV-3 (recovery completeness) so depth-limited trees still satisfy full-input byte coverage? [Consistency, data-model §INV-5/INV-3] <!-- Evaluator: Resolved — INV-5 amended to state the depth guard stops only recursive descent, not tokenization, so INV-1/INV-2/INV-3 hold and depth-limited trees still cover all input and round-trip -->

## Termination Guarantee

- [X] CHK013 Is the termination guarantee stated for *any* input (valid, malformed, and arbitrary bytes), not just valid RON? [Coverage, Spec §TR-014/SC-003] <!-- Evaluator: Covered by TR-014 ("MUST terminate on all input") and SC-003 ("every accepted UTF-8 input — valid or malformed") -->
- [X] CHK014 Is the termination guarantee testable — does the spec give a measurable success condition (parsing completes and token-text concatenation equals input) rather than asserting termination abstractly? [Testability, Spec §SC-003, Obj2 VC2] <!-- Evaluator: Covered by SC-003 and Obj2 Validation Criterion 2 ("parsing terminates and concatenating the tree's token texts equals the input") -->
- [X] CHK015 Is the must-consume-a-token / per-iteration-progress invariant that underpins termination captured as a requirement-level guarantee, or is it left implicit in a plan hint? [Completeness, Spec §Obj2 Rationale, plan §HINT-004] <!-- Evaluator: Covered — termination is a hard requirement (TR-014); the underpinning must-consume-a-token invariant is documented in spec §Obj2 Rationale (spec-level, not only plan §HINT-004) -->
- [X] CHK016 Does the termination requirement cover the non-UTF-8 rejection path (clean error, never a hang or panic at the boundary) as well as the accepted-input path? [Coverage, Spec §TR-001, plan §Error Handling] <!-- Evaluator: Covered by TR-001 + SC-003 ("non-UTF-8 input is rejected without panicking") + plan §Error Handling ("Non-UTF-8 → clean Err, no tree, no panic, Retry: no") -->

## Large / Deeply-Nested File Handling Boundaries

- [X] CHK017 Is "very large and deeply nested files" defined with a discriminating boundary (what counts as the scaling axis) rather than left as an open-ended edge case? [Clarity, Spec §Edge Cases, plan §Scale/Scope] <!-- Resolved: size vs depth axes separated; depth bounded by TR-014; corpus includes a ≥1 MB representative large file (SC-001) -->
- [X] CHK018 Does the spec state the *only* committed behavior for large files (correct round-trip + termination, no responsiveness promise) so reviewers do not expect a latency outcome? [Clarity, Spec §Edge Cases, plan §Performance Goals] <!-- Evaluator: Resolved — spec §Edge Cases now states the only committed behavior for both size and depth axes is correct round-trip + termination, with no responsiveness/latency/throughput promise -->
- [X] CHK019 Is the distinction between the "large file" axis (size) and the "deeply nested" axis (depth, bounded by TR-014) made explicit so the two boundaries are not conflated? [Clarity, Spec §Edge Cases/TR-014, plan §Scale/Scope] <!-- Evaluator: Resolved — spec §Edge Cases now names the size axis (no committed bound) and the depth axis (bounded by TR-014) as two distinct, non-conflated axes -->
- [X] CHK020 Are large-file and deep-nesting cases mapped to a verifiable success criterion or success-criteria gap explicitly acknowledged, rather than silently uncovered? [Coverage, Spec §SC-008/Edge Cases] <!-- Evaluator: Resolved — spec §Edge Cases maps depth to SC-008 and size to SC-001/SC-003, and explicitly acknowledges that size has no dedicated threshold SC by design (correctness-only) -->
- [X] CHK021 Is memory behavior for large/deeply-nested input addressed at least to the extent of stating it is *out of scope as a threshold* (consistent with correctness-only)? [Completeness, plan §Performance Goals/Constraints] <!-- Evaluator: Resolved — spec §Edge Cases and plan §Performance Goals now state memory is explicitly out of scope as a threshold (no latency/throughput/memory threshold) -->

## Benchmark-Harness Expectation (No Pass/Fail Gate)

- [X] CHK022 Is the benchmark harness stated as an established deliverable while explicitly carrying no pass/fail gate, so neither absence nor presence of a threshold is ambiguous? [Clarity, Spec §Clarifications Q6, plan §Performance Goals] <!-- Evaluator: Covered by Clarifications Q6 + plan §Performance Goals ("Benchmark harness established, no pass/fail gate"); now also formalized as TR-016 -->
- [X] CHK023 Is the no-gate status of the benchmark harness consistent across the spec, plan, and project-instructions (which mark performance "not yet a hard QC gate")? [Consistency, plan §Performance Goals, project-instructions §Performance Standards] <!-- Evaluator: Covered — spec (Q6/TR-016), plan §Performance Goals, and project-instructions §Performance Standards ("Not yet a hard QC gate") all agree on no gate -->
- [X] CHK024 Does any requirement, success criterion, or QC category claim a performance pass/fail that would contradict the "no gate" decision? [Consistency, Spec §Success Criteria, plan §Testing Strategy] <!-- Evaluator: Covered — SC-001..008 reference no latency/throughput number; plan §Testing Strategy mandatory gates are fmt/clippy/test/wasm only; TR-016 explicitly asserts no pass/fail gate -->
- [X] CHK025 Is the benchmark harness's purpose (track/inform later epics) distinguished from a release-gating role, so QC does not erroneously treat it as a Release-Ready criterion? [Clarity, plan §Performance Goals, QA §Definition of Done] <!-- Evaluator: Resolved — plan §Performance Goals now states the harness's sole purpose is to track/inform later epics (E003+) and carries NO release-gating / Definition-of-Done / QC role; TR-016 echoes this -->
- [X] CHK026 Is the benchmark harness expectation traceable to a requirement or deliverable, or is it noted only in a clarification and therefore at risk of being dropped from tasks? [Traceability, Spec §Clarifications Q6, plan §Testing Strategy] <!-- Evaluator: Resolved — added TR-016 (benchmark harness) to spec.md and to plan §Requirement Coverage Map + §Verification Mechanism Map, so the harness is now traceable rather than clarification-only -->

## Determinism vs Performance

- [X] CHK027 Is determinism (TR-012, INV-6 — identical input yields identical tree and diagnostics) stated independently of any performance characteristic, so it is not conflated with speed? [Clarity, Spec §TR-012, data-model §INV-6] <!-- Evaluator: Covered — TR-012 (now "independent of any performance characteristic") and INV-6 define determinism as input→output equality with no speed coupling -->
- [X] CHK028 Does the determinism requirement scope cover both the tree *and* the diagnostics (including over-limit/recovery diagnostics) so reproducibility is complete? [Completeness, Spec §TR-012, data-model §INV-6] <!-- Evaluator: Resolved — TR-012 amended to require an identical diagnostics set (including recovery and over-limit diagnostics) in addition to an identical tree, matching INV-6 -->
- [X] CHK029 Is determinism expressed as a verifiable property (same input → byte/structure-identical output) rather than as a quality goal? [Testability, data-model §INV-6, Spec §TR-012] <!-- Evaluator: Covered by INV-6 ("same kinds, order, ranges") + plan §Verification Mechanism Map TR-012 ("determinism: re-parse equality, snapshot stability") -->
- [X] CHK030 Is there any tension between determinism and a deferred-incremental-reparse note that needs explicit reconciliation (full reparse remains deterministic)? [Consistency, Spec §Assumptions, plan §Constraints] <!-- Evaluator: Covered — incremental reparse is deferred (spec §Excluded/Assumptions), so E001 performs full reparse, which is a parse and therefore deterministic under TR-012/INV-6; no live tension -->

## Scope Clarity — Incremental Reparse Deferred

- [X] CHK031 Is the deferral of incremental/partial reparse stated unambiguously as out of scope for E001, with the owning future scope identified or marked deferred? [Clarity, Spec §Excluded] <!-- Evaluator: Covered by spec §Excluded ("Incremental/partial reparse — deferred; E001 may fully reparse on change") — explicitly marked deferred -->
- [X] CHK032 Does the spec state that full reparse on change is acceptable for this epic, and that correctness (round-trip, editing) is unaffected by the deferral? [Completeness, Spec §Excluded/Assumptions, plan §Performance Goals] <!-- Evaluator: Covered by spec §Excluded ("E001 may fully reparse on change. Re-printing and editing remain correct regardless") + §Assumptions ("incremental reparse is deferred without affecting correctness") -->
- [X] CHK033 Is the incremental-reparse deferral consistent across spec Scope, Assumptions, and plan so no requirement implicitly assumes incremental behavior? [Consistency, Spec §Excluded/Assumptions, plan §Constraints] <!-- Evaluator: Covered — spec §Excluded, §Assumptions, and plan all assert full reparse / deferred; no TR assumes incremental behavior -->
- [X] CHK034 Does the project-instructions performance note (incremental reparse of the edited region) get reconciled against this epic's explicit deferral, so the conflict is acknowledged rather than latent? [Consistency, project-instructions §Performance Standards, Spec §Excluded] <!-- Resolved (PASS, no edit): project-instructions §Performance Standards is already scoped to the desktop editor ("Targets for the desktop editor"); Spec §Excluded defers incremental reparse for this library epic — no contradiction -->

## Traceability & Cross-Artifact Consistency

- [X] CHK035 Does every performance-envelope guarantee (termination, bounded depth, no-gate benchmark, determinism) trace to at least one TR-/SC-/AD-/INV- identifier? [Traceability, Spec §TR-012/TR-014/SC-003/SC-008, plan §AD-005, data-model §INV-5/INV-6] <!-- Evaluator: Covered — termination→TR-014/SC-003; bounded depth→TR-014/SC-008/AD-005/INV-5; determinism→TR-012/INV-6; no-gate benchmark now→TR-016 (added), closing the prior traceability gap -->
- [X] CHK036 Is AD-005 (recursion/depth-safety decision) consistent with TR-014 and SC-008 in both the bound mechanism and the over-limit behavior? [Consistency, plan §AD-005, Spec §TR-014/SC-008] <!-- Evaluator: Covered — AD-005 (nesting-depth guard emitting a diagnostic past the limit) matches TR-014/SC-008 (documented bound + diagnostic on exceed + no overflow) -->
- [X] CHK037 Are SC-008 and TR-014 mutually consistent (same bound concept, same diagnostic-on-exceed outcome) with no divergent wording that could yield conflicting acceptance tests? [Consistency, Spec §SC-008/TR-014] <!-- Evaluator: Covered — both state a documented nesting bound, a diagnostic when exceeded, and no stack overflow; no divergent wording -->
- [X] CHK038 Is the Requirement Coverage Map's TR-014 row sufficient to verify the no-stack-overflow guarantee, or does it omit the over-limit diagnostic/termination linkage? [Completeness, plan §Requirement Coverage Map] <!-- Evaluator: Resolved — plan §Requirement Coverage Map TR-014 row Notes amended to include the over-limit Diagnostic (TR-013 contract), termination, and full byte coverage (INV-5/INV-3) -->
- [X] CHK039 Are the Error Handling Strategy rows for "Excessive nesting depth" and "Non-UTF-8 input" consistent with TR-001/TR-014 and the correctness-only envelope (no retry, clean signal)? [Consistency, plan §Error Handling, Spec §TR-001/TR-014] <!-- Evaluator: Covered — plan §Error Handling rows (nesting: stop descent + diagnostic + round-trip, Retry no; non-UTF-8: clean Err, no panic, Retry no) align with TR-001/TR-014 and correctness-only -->
- [X] CHK040 Is each performance-related success criterion (SC-003, SC-008) measurable without reference to a latency/throughput number, confirming the correctness-only framing is testable as written? [Testability, Spec §SC-003/SC-008] <!-- Evaluator: Covered — SC-003 (token-text concat == input, no panics) and SC-008 (no overflow, documented bound, diagnostic on exceed) are measurable with no latency/throughput number -->
