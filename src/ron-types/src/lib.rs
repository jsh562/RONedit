//! `ron-types` — normalized RON type model for RONin.
//!
//! This is a placeholder stub crate. The full implementation (schema-optional
//! type model, `syn`/`schemars` extraction, validation hooks) is deferred to
//! epic E004. It exists now only so the Cargo workspace builds (TR-008).

#![forbid(unsafe_code)]
