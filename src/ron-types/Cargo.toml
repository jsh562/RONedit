[package]
name = "ron-types"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Normalized RON type model for RONin (stub; full implementation deferred to E004)."

[dependencies]
