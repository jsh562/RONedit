//! `ronin-app` — RONin desktop application shell.
//!
//! This is a placeholder stub binary. The full desktop editor (egui/eframe UI,
//! filesystem I/O, autosave/recovery) is deferred to epic E003. It exists now
//! only so the Cargo workspace builds (TR-008).

fn main() {
    println!("ronin-app: stub binary (desktop editor deferred to E003).");
}
